# Base path for quantity API calls (proxied in dev via vite.config.js)
VITE_API_BASE_URL=/api/quantity
# Full backend origin, needed for the Google OAuth2 redirect (real browser navigation, not proxied)
VITE_BACKEND_ORIGIN=http://localhost:8080